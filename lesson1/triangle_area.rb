puts 'OK, now we are going to calculate area of a triangle.'

print 'Enter base: '
base = gets.strip.to_f

print 'Enter hight: '
hight = gets.strip.to_f

area = (base / 2) * hight

puts "Area of the triangle: #{area}"
